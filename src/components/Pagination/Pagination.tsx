import React from 'react';
import styles from './style.module.scss';

function Pagination(props: {setPagination: any}) {
  return (
    <section className={styles.paginationBox}>
      Pagination
    </section>
  );
}

export default Pagination;